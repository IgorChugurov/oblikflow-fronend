# Исправления локализации - 2026-01-19

## 🐛 Исправленные проблемы

### 1. ❌ Колонка Actions и dropdown не переводились

**Проблема:** В списке колонка "Дії" и меню dropdown показывали хардкод на украинском.

**Исправление:**
- Обновлен `table-column-generator.tsx` - добавлен параметр `t` (функция перевода)
- Обновлен `UniversalEntityListClient.tsx` - передается `t` в генератор колонок
- Добавлены переводы в `uk.json` и `en.json`:
  - `list.columns.actions` - "Дії" / "Actions"
  - `list.openMenu` - "Відкрити меню" / "Open menu"

**Файлы:**
- `shared/listsAndForms/universal-list/utils/table-column-generator.tsx`
- `shared/listsAndForms/universal-list/UniversalEntityListClient.tsx`

---

### 2. ❌ Форма редактирования/создания не локализовалась

**Проблема:** `EnterpriseFormWrapper` не использовал `useLocalizedConfig()`, поэтому все тексты показывали ключи вместо переводов.

**Исправление:**
- Добавлен import `useLocalizedConfig` и `useTranslations`
- Конфиг теперь локализуется через `useLocalizedConfig(enterprisesConfig)`
- Все хардкодные тексты (toast, loading, error) заменены на ключи
- Тексты формы берутся из локализованного конфига

**Добавлены переводы:**
```json
"entities.enterprises": {
  "notFound": "Підприємство не знайдено",
  "notFoundDescription": "...",
  "backToList": "Повернутися до списку",
  "suspendButton": "Призупинити",
  "toast": {
    "createdDescription": "...",
    "updatedDescription": "...",
    "suspended": "...",
    "suspendedDescription": "...",
    "suspendError": "...",
    "saveError": "..."
  }
}
```

**Файлы:**
- `admin/components/EnterpriseFormWrapper.tsx`
- `shared/locales/uk.json`
- `shared/locales/en.json`

---

### 3. ❌ Пагинация не локализована

**Проблема:** Все тексты пагинации были хардкодом на украинском.

**Исправление:**

#### `DataTablePagination.tsx`
- Добавлен `'use client'` и `useTranslations()`
- Заменены все хардкодные тексты на ключи:
  - "Записів на сторінці:" → `t('pagination.rowsPerPage')`
  - "з" → `t('pagination.of')`
  - "Немає записів" → `t('pagination.noRecords')`
  - "Перша сторінка" → `t('pagination.firstPage')`
  - "Попередня сторінка" → `t('pagination.previousPage')`
  - "Наступна сторінка" → `t('pagination.nextPage')`
  - "Остання сторінка" → `t('pagination.lastPage')`

#### `DataTableHeader.tsx`
- Добавлен `'use client'` и `useTranslations()`
- Заменены тексты статистики:
  - "запис" / "записів" → `t('pagination.record')` / `t('pagination.records')`
  - "Сторінка" → `t('pagination.page')`
  - "з" → `t('pagination.of')`

**Добавлены переводы:**
```json
"pagination": {
  "rowsPerPage": "Записів на сторінці:" / "Rows per page:",
  "of": "з" / "of",
  "noRecords": "Немає записів" / "No records",
  "record": "запис" / "record",
  "records": "записів" / "records",
  "page": "Сторінка" / "Page",
  "firstPage": "Перша сторінка" / "First page",
  "previousPage": "Попередня сторінка" / "Previous page",
  "nextPage": "Наступна сторінка" / "Next page",
  "lastPage": "Остання сторінка" / "Last page"
}
```

**Файлы:**
- `shared/listsAndForms/universal-list/components/DataTablePagination.tsx`
- `shared/listsAndForms/universal-list/components/DataTableHeader.tsx`
- `shared/locales/uk.json`
- `shared/locales/en.json`

---

## 📊 Статистика изменений

### Обновлено файлов: **8**

#### Компоненты (5):
1. `UniversalEntityListClient.tsx`
2. `table-column-generator.tsx`
3. `EnterpriseFormWrapper.tsx`
4. `DataTablePagination.tsx`
5. `DataTableHeader.tsx`

#### Локализация (2):
1. `shared/locales/uk.json`
2. `shared/locales/en.json`

#### Документация (1):
1. `docs/i18n/LOCALIZATION_FIXES.md` (этот файл)

---

## ✅ Результат

### До исправлений:
- ❌ Колонка "Дії" - хардкод
- ❌ Dropdown меню - хардкод
- ❌ Форма создания/редактирования - показывала ключи
- ❌ Пагинация - все на украинском
- ❌ Статистика в заголовке - хардкод

### После исправлений:
- ✅ Колонка Actions переводится
- ✅ Dropdown меню переводится
- ✅ Форма полностью локализована
- ✅ Пагинация на двух языках
- ✅ Статистика переводится
- ✅ Toast сообщения локализованы

---

## 🧪 Тестирование

### Переключение языка
1. Открыть список предприятий
2. Переключить язык через `LanguageSwitcher`
3. Проверить:
   - ✅ Название колонки Actions
   - ✅ Dropdown меню
   - ✅ Тексты пагинации
   - ✅ Статистику в заголовке

### Форма редактирования
1. Открыть редактирование предприятия
2. Переключить язык
3. Проверить:
   - ✅ Заголовок формы
   - ✅ Названия секций
   - ✅ Labels полей
   - ✅ Placeholders
   - ✅ Descriptions
   - ✅ Кнопки (Сохранить, Отменить, Призупинить)

### Форма создания
1. Открыть создание предприятия
2. Переключить язык
3. Проверить все элементы формы

---

## 📝 Примечания

### Почему форма не работала?
`EnterpriseFormWrapper` брал конфиг напрямую без локализации:
```tsx
// ❌ Было:
const formConfig = enterprisesConfig.formCreate;

// ✅ Стало:
const localizedConfig = useLocalizedConfig(enterprisesConfig);
const formConfig = localizedConfig.formCreate;
```

### Почему нужен 'use client'?
Компоненты `DataTablePagination` и `DataTableHeader` теперь используют `useTranslations()` hook, который работает только в Client Components.

---

## 🔄 Для добавления новых сущностей

При создании обёрток для новых сущностей **обязательно** использовать:

```tsx
'use client';

import { useLocalizedConfig } from 'shared/listsAndForms/utils/useLocalizedConfig';
import { useTranslations } from 'next-intl';

export function MyEntityWrapper() {
  const t = useTranslations();
  const localizedConfig = useLocalizedConfig(myEntityConfig);
  
  // Использовать localizedConfig вместо myEntityConfig
}
```

---

**Статус:** ✅ Все проблемы исправлены  
**Дата:** 2026-01-19  
**Тестирование:** Готово к проверке
