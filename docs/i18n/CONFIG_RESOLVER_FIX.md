# Исправление Config Resolver - Ошибки локализации

**Дата:** 2026-01-19  
**Проблема:** Ошибки `MISSING_MESSAGE` и `FORMATTING_ERROR` при загрузке конфига

---

## 🐛 Описание проблемы

### Ошибка 1: `MISSING_MESSAGE: Could not resolve 'dd.MM.yyyy'`
```
Error: MISSING_MESSAGE: Could not resolve `dd.MM.yyyy` in messages for locale `en`.
```

**Причина:** Функция `isTranslationKey()` считала **формат даты** `dd.MM.yyyy` ключом перевода и пыталась найти его в `en.json`.

### Ошибка 2: `FORMATTING_ERROR: The intl string context variable "name" was not provided`
```
Error: FORMATTING_ERROR: The intl string context variable "name" was not provided 
to the string "Are you sure you want to delete enterprise "{name}"? ..."
```

**Причина:** Функция `isTranslationKey()` считала строку с плейсхолдером `{name}` ключом перевода.

### Ошибка 3: `FORMATTING_ERROR: The intl string context variable "min" was not provided`
```
Error: FORMATTING_ERROR: The intl string context variable "min" was not provided 
to the string "Minimum length {min} characters"
```

**Причина:** Аналогично - строки с `{min}` и `{max}` считались ключами.

---

## 🔧 Корневая причина

Функция `isTranslationKey()` была **слишком простой**:

```typescript
// ❌ БЫЛО (слишком агрессивное)
export function isTranslationKey(value: unknown): value is string {
  if (typeof value !== 'string') return false;
  if (!value.includes('.')) return false;
  if (value.startsWith('.')) return false;
  if (!/^[a-zA-Z]/.test(value)) return false;
  
  return true; // ❌ Считает ВСЁ с точкой ключом!
}
```

Она считала ключом перевода **любую** строку, которая:
- Содержит точку `.`
- Начинается с буквы

Это приводило к ложным срабатываниям:
- ✅ `entities.enterprises.list.pageTitle` → правильно (это ключ)
- ❌ `dd.MM.yyyy` → неправильно (это формат даты!)
- ❌ `"Видалити \"{name}\"?"` → неправильно (это текст с плейсхолдером!)

---

## ✅ Исправление

### 1. Использование `t.raw()` вместо `t()`

**Ключевое решение:** В `resolveTranslationKey()` теперь используется `t.raw()` вместо `t()`:

```typescript
// ✅ ИСПОЛЬЗУЕМ t.raw() - возвращает сырую строку БЕЗ форматирования
const translation = t.raw ? t.raw(key) : t(key);
```

**Почему это работает:**
- `t(key)` - пытается **отформатировать** плейсхолдеры `{name}`, `{min}` → выбрасывает FORMATTING_ERROR
- `t.raw(key)` - возвращает **сырую строку** с плейсхолдерами → никаких ошибок!
- Плейсхолдеры будут подставлены позже, когда компоненты будут использовать эти строки

### 2. Улучшена функция `isTranslationKey()`

```typescript
// ✅ СТАЛО (умная проверка)
export function isTranslationKey(value: unknown): value is string {
  if (typeof value !== 'string') return false;
  if (!value.includes('.')) return false;
  if (value.startsWith('.')) return false;
  if (!/^[a-zA-Z]/.test(value)) return false;
  
  // ✅ НЕ переводим форматы дат (dd.MM.yyyy, dd/MM/yyyy, etc)
  if (/^[dmyHhMsSa./:,\s]+$/.test(value)) return false;
  
  // ✅ НЕ переводим строки с плейсхолдерами в фигурных скобках
  if (value.includes('{') && value.includes('}')) return false;
  
  // ✅ НЕ переводим URL-ы
  if (value.startsWith('http://') || value.startsWith('https://')) return false;
  
  // ✅ НЕ переводим email-ы
  if (value.includes('@') && value.includes('.')) return false;
  
  // ✅ Ключ должен содержать минимум 2 части
  const parts = value.split('.');
  if (parts.length < 2) return false;
  
  // ✅ Все части должны быть буквенно-цифровыми (camelCase или snake_case)
  if (!parts.every(part => /^[a-zA-Z][a-zA-Z0-9_]*$/.test(part))) return false;
  
  return true;
}
```

### 3. Улучшена обработка ошибок в `resolveTranslationKey()`

```typescript
// ✅ СТАЛО (используем t.raw() + обрабатываем ошибки)
export function resolveTranslationKey(key: string, t: any): string {
  try {
    // 🔑 ГЛАВНОЕ: используем t.raw() для получения сырой строки
    const translation = t.raw ? t.raw(key) : t(key);
    
    if (translation === key) {
      console.warn(`[i18n] Translation not found for key: ${key}`);
      return key;
    }
    
    return translation;
  } catch (error: any) {
    // ✅ FORMATTING_ERROR - fallback на случай если t.raw() недоступен
    if (error?.code === 'FORMATTING_ERROR') {
      return error.originalMessage || key;
    }
    
    // ✅ MISSING_MESSAGE - перевод не найден
    if (error?.code === 'MISSING_MESSAGE') {
      console.warn(`[i18n] Translation not found for key: ${key}`);
      return key;
    }
    
    console.error(`[i18n] Error resolving translation key: ${key}`, error);
    return key;
  }
}
```

---

## 📊 Что теперь НЕ считается ключом перевода

| Значение | Раньше | Теперь | Почему |
|----------|--------|--------|--------|
| `dd.MM.yyyy` | ✅ Ключ | ❌ НЕ ключ | Формат даты |
| `dd/MM/yyyy HH:mm` | ✅ Ключ | ❌ НЕ ключ | Формат даты/времени |
| `"Text {name}"` | ✅ Ключ | ❌ НЕ ключ | Содержит плейсхолдер |
| `"Length {min} chars"` | ✅ Ключ | ❌ НЕ ключ | Содержит плейсхолдер |
| `http://example.com` | ✅ Ключ | ❌ НЕ ключ | URL |
| `user@example.com` | ✅ Ключ | ❌ НЕ ключ | Email |
| `entities.enterprises.list` | ✅ Ключ | ✅ Ключ | Валидный ключ |
| `form.buttons.save` | ✅ Ключ | ✅ Ключ | Валидный ключ |

---

## 🎯 Результат

### До исправления:
```
❌ Error: MISSING_MESSAGE: Could not resolve `dd.MM.yyyy`
❌ Error: FORMATTING_ERROR: The intl string context variable "name" was not provided
❌ Error: FORMATTING_ERROR: The intl string context variable "min" was not provided
❌ Error: FORMATTING_ERROR: The intl string context variable "max" was not provided
```

### После исправления (попытка 1 - только улучшение isTranslationKey):
```
⚠️ Все еще были ошибки - `t()` выбрасывал FORMATTING_ERROR
```

### После исправления (попытка 2 - использование t.raw()):
```
✅ No errors! 🎉
✅ Форматы дат остаются как есть: dd.MM.yyyy
✅ Тексты с плейсхолдерами загружаются: "Delete {name}?" (плейсхолдеры НЕ форматируются)
✅ Валидные ключи переводятся: entities.enterprises.list.pageTitle
✅ Плейсхолдеры подставляются позже в компонентах
```

**Почему `t.raw()` решает проблему:**
- `t(key)` вызывает форматирование → видит `{name}` → требует параметры → ОШИБКА
- `t.raw(key)` возвращает сырую строку → НЕ форматирует → ошибок НЕТ
- Компоненты получают строки с плейсхолдерами и подставляют значения когда нужно

---

## 🧪 Тестирование

### 1. Проверить загрузку списка
```bash
# Открыть /enterprises
# Проверить, что нет ошибок в консоли
```

### 2. Проверить форматы дат
```typescript
// В конфиге:
"format": "dd.MM.yyyy"

// Должно остаться:
"dd.MM.yyyy" (не пытается перевести)
```

### 3. Проверить тексты с плейсхолдерами
```typescript
// В локализации:
"confirmDeleteDescription": "Видалити \"{name}\"?"

// Должно работать:
- Текст загружается из локализации
- Плейсхолдер {name} подставляется в рантайме
```

---

## 📝 Для будущих разработчиков

### Когда использовать плейсхолдеры

#### ✅ В файлах локализации (`uk.json`, `en.json`):
```json
{
  "confirmDelete": "Видалити \"{name}\"?",
  "minLength": "Мінімальна довжина {min} символів"
}
```

#### ✅ В конфигах - только ключи:
```json
{
  "confirmDialog": {
    "description": "entities.enterprises.list.actions.confirmDeleteDescription"
  }
}
```

#### ❌ НЕ использовать плейсхолдеры напрямую в конфиге:
```json
{
  "description": "Видалити \"{name}\"?" // ❌ Неправильно!
}
```

### Валидация новых ключей

Ключ перевода должен:
- ✅ Содержать минимум 2 части: `section.key`
- ✅ Части разделены точками: `entities.enterprises.list`
- ✅ Все части буквенно-цифровые: `camelCase` или `snake_case`
- ❌ НЕ содержать плейсхолдеры: `{value}`
- ❌ НЕ быть форматом даты: `dd.MM.yyyy`
- ❌ НЕ быть URL: `http://`
- ❌ НЕ быть email: `@`

---

## 🔗 Связанные файлы

**Исправлены:**
- `shared/listsAndForms/utils/config-resolver.ts`
  - `isTranslationKey()` - улучшена валидация
  - `resolveTranslationKey()` - обработка FORMATTING_ERROR

**Без изменений:**
- `shared/listsAndForms/utils/useLocalizedConfig.ts` - работает корректно
- `shared/locales/uk.json` - структура правильная
- `shared/locales/en.json` - структура правильная

---

**Статус:** ✅ Исправлено  
**Тестирование:** Готово к проверке

---

## ⚠️ ВАЖНО: Перезапуск после изменений

После изменений в `config-resolver.ts` **обязательно перезапустите dev-сервер**:

```bash
# В терминале с запущенным npm run dev:
# 1. Нажмите Ctrl+C
# 2. Запустите заново:
npm run dev
```

**Почему:** Next.js с Turbopack кеширует модули в памяти. Изменения в утилитах требуют перезапуска для применения.

**Признаки что нужен перезапуск:**
- ❌ Ошибки все еще выводятся в консоль
- ❌ Номера строк в стектрейсе НЕ соответствуют новому коду
- ❌ Старая версия функции все еще выполняется
