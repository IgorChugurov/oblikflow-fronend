# Локализация конфигурационных файлов - Реализация

**Дата:** 2026-01-19  
**Подход:** Централизованная локализация через next-intl  
**Языки:** Украинский (uk), Английский (en)

---

## 📋 Что было реализовано

### ✅ **Этап 1: Расширение файлов локализации**

Обновлены файлы:
- `shared/locales/uk.json`
- `shared/locales/en.json`

#### Добавлены новые секции:

1. **`list.columns`** - общие названия колонок
   ```json
   "list": {
     "columns": {
       "actions": "Дії" / "Actions"
     }
   }
   ```

2. **`form.buttons`** - переиспользуемые кнопки
   ```json
   "form": {
     "buttons": {
       "submit": "Відправити",
       "cancel": "Скасувати",
       "save": "Зберегти",
       "saveChanges": "Зберегти зміни",
       "create": "Створити",
       "delete": "Видалити",
       "edit": "Редагувати",
       "close": "Закрити",
       "back": "Назад"
     }
   }
   ```

3. **`form.sections`** - общие секции форм
   ```json
   "form": {
     "sections": {
       "basicInfo": {
         "title": "Основна інформація",
         "description": "Загальні дані"
       },
       "accountingSettings": {
         "title": "Налаштування обліку",
         "description": "Валюта та мова інтерфейсу"
       }
     }
   }
   ```

4. **`entities.enterprises`** - специфичные тексты для enterprises
   - `list` - тексты для списка
   - `formCreate` - тексты для формы создания
   - `formEdit` - тексты для формы редактирования

---

### ✅ **Этап 2: Создание утилит локализации**

Созданы файлы:

#### `shared/listsAndForms/utils/config-resolver.ts`
Функции:
- `isTranslationKey()` - проверка, является ли строка ключом перевода
- `resolveTranslationKey()` - получение перевода по ключу
- `localizeConfigObject()` - рекурсивная локализация объекта
- `getNestedValue()` - доступ к вложенным значениям

#### `shared/listsAndForms/utils/useLocalizedConfig.ts`
React Hook:
- `useLocalizedConfig<T>(config: T): T` - локализует конфиг на текущем языке
- Автоматически реагирует на изменение локали
- Мемоизирует результат

---

### ✅ **Этап 3: Обновление конфигурационного файла**

Файл: `shared/listsAndForms/configuration-setup/enterprises.config.json`

Все тексты заменены на ключи:

**Было:**
```json
{
  "pageTitle": "Мої підприємства",
  "createButtonText": "Створити підприємство"
}
```

**Стало:**
```json
{
  "pageTitle": "entities.enterprises.list.pageTitle",
  "createButtonText": "entities.enterprises.list.createButtonText"
}
```

#### Использование общих ключей:

- Кнопки: `form.buttons.cancel`, `form.buttons.save`, `form.buttons.edit`, `form.buttons.delete`
- Секции: `form.sections.basicInfo.title`, `form.sections.accountingSettings.title`

---

### ✅ **Этап 4: Обновление компонентов**

#### `UniversalEntityListClient.tsx`
Добавлен `useTranslations()`:
- ❌ `"цей запис"` → ✅ `t('common.thisRecord')`
- ❌ `"Помилка видалення"` → ✅ `t('common.deleteError')`
- ❌ `"Видалення..."` → ✅ `t('common.deleting')`

#### `FormRenderer.tsx`
Добавлен `useTranslations()`:
- ❌ `"Створити"` → ✅ `t('form.create')`
- ❌ `"Зберегти"` → ✅ `t('form.save')`
- ❌ `"Скасувати"` → ✅ `t('form.buttons.cancel')`
- ❌ `"Збереження..."` → ✅ `t('form.saving')`

#### `SelectField.tsx`
Добавлен `useTranslations()`:
- ❌ `"Завантаження..."` → ✅ `t('form.loading')`
- ❌ `"Немає опцій"` → ✅ `t('common.noOptions')`
- ❌ `"Оберіть значення"` → ✅ `t('form.selectValue')`

---

### ✅ **Этап 5: Обновление обёрток**

#### `EnterprisesListWrapper.tsx`
Добавлено использование `useLocalizedConfig`:

**Было:**
```tsx
const listSpec = enterprisesConfig.list as ListSpec;
```

**Стало:**
```tsx
const localizedConfig = useLocalizedConfig(enterprisesConfig);
const listSpec = localizedConfig.list as ListSpec;
```

---

## 🎯 Как это работает

### 1. **Загрузка конфига**
```tsx
import enterprisesConfig from './enterprises.config.json';
```

### 2. **Локализация через hook**
```tsx
const localizedConfig = useLocalizedConfig(enterprisesConfig);
```

### 3. **Автоматическая замена ключей**
Hook рекурсивно проходит по конфигу и заменяет строки-ключи (например, `"entities.enterprises.list.pageTitle"`) на переводы из `shared/locales/{locale}.json`.

### 4. **Реактивность**
При переключении языка через `LanguageSwitcher`, hook автоматически пересчитывает локализованный конфиг.

---

## 📦 Структура переводов

```
shared/
├── locales/
│   ├── uk.json
│   │   ├── common (общие тексты)
│   │   ├── list (списки)
│   │   ├── form (формы)
│   │   │   ├── buttons (кнопки)
│   │   │   └── sections (секции)
│   │   └── entities
│   │       └── enterprises (специфичные тексты)
│   └── en.json (аналогичная структура)
│
├── listsAndForms/
│   ├── utils/
│   │   ├── config-resolver.ts
│   │   └── useLocalizedConfig.ts
│   └── configuration-setup/
│       └── enterprises.config.json (ключи вместо текстов)
```

---

## 🔄 Как добавить новую сущность

### 1. Создать конфиг с ключами
```json
{
  "list": {
    "pageTitle": "entities.products.list.pageTitle",
    "createButtonText": "entities.products.list.createButtonText"
  }
}
```

### 2. Добавить переводы в `uk.json` и `en.json`
```json
{
  "entities": {
    "products": {
      "list": {
        "pageTitle": "Товари",
        "createButtonText": "Створити товар"
      }
    }
  }
}
```

### 3. Использовать общие ключи где возможно
```json
{
  "ui": {
    "submitButtonText": "entities.products.formCreate.submitButtonText",
    "cancelButtonText": "form.buttons.cancel" // общий ключ
  }
}
```

### 4. В обертке использовать hook
```tsx
const localizedConfig = useLocalizedConfig(productsConfig);
```

---

## ✨ Преимущества реализации

1. **Централизация** - все переводы в одном месте (`shared/locales/`)
2. **Переиспользование** - общие тексты (кнопки, секции) не дублируются
3. **Простота** - одна строка кода для локализации конфига
4. **Масштабируемость** - легко добавить новый язык (просто файл JSON)
5. **Типобезопасность** - TypeScript контролирует структуру
6. **Реактивность** - автоматическое обновление при смене языка

---

## 🚀 Дальнейшие шаги

### Для добавления нового языка (например, польский):

1. Создать `shared/locales/pl.json` (скопировать структуру из `uk.json`)
2. Перевести все тексты на польский
3. Язык автоматически станет доступен (конфиг уже в `shared/lib/i18n/config.ts`)

### Для локализации других конфигов:

1. Заменить тексты на ключи в `.config.json`
2. Добавить переводы в `uk.json` и `en.json`
3. Использовать `useLocalizedConfig()` в обертке
4. Готово! ✅

---

## 📝 Примечания

- **Fallback:** Если перевод не найден, показывается сам ключ (для отладки)
- **Performance:** Hook мемоизирован, пересчет только при смене локали
- **Validation:** В конфиге тоже используются ключи переводов для сообщений об ошибках

---

**Автор:** AI Assistant  
**Статус:** ✅ Готово к использованию
