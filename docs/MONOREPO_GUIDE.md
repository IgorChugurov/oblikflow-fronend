# 📘 Monorepo Management Guide

**Дата:** 2026-01-16  
**Версия:** 1.0.0

---

## 🏗️ Архитектура монорепозитория

### Структура проекта

```
oblikflow-frontend/
├── site/           → Публичный сайт (port 3000)
├── admin/          → Админ-панель (port 3001)
├── workspace/      → Рабочее пространство (port 3002)
├── platform/       → Платформа SuperAdmin (port 3003)
├── shared/         → Общая библиотека (компоненты, утилиты, SDK)
├── tsconfig.base.json  → Базовая конфигурация TypeScript
├── pnpm-workspace.yaml → Конфигурация workspace
└── .npmrc          → Настройки pnpm
```

### Принципы архитектуры

1. **Изоляция приложений**: Каждое приложение — независимый Next.js проект
2. **Общий код в shared**: UI компоненты, auth SDK, утилиты
3. **Единый источник истины**: TypeScript конфиг, линтер, стили
4. **Независимый деплой**: Каждое приложение деплоится отдельно на Vercel

---

## 📦 Управление зависимостями

### Классификация зависимостей

#### 🔧 Build-time зависимости

**Определение:** Используются в конфигурационных файлах, выполняющихся ДО загрузки приложения

**Примеры:**
- `next.config.ts` → импортирует `next-intl/plugin`
- `middleware.ts` → импортирует модули напрямую

**Где добавлять:**
- ✅ В `package.json` КАЖДОГО приложения (site, admin, workspace, platform)
- ✅ Также в `shared/package.json` (если используется в shared коде)

**Почему дублировать:**
- Next.js выполняет `next.config.ts` ДО загрузки workspace зависимостей
- С настройкой `hoist=false` symlink не работает для конфигов
- Vercel собирает каждое приложение изолированно
- pnpm deduplicate автоматически создаст symlink на одну версию

**Текущий список:**
```json
{
  "dependencies": {
    "next-intl": "^4.7.0"
  }
}
```

---

#### 🚀 Runtime зависимости

**Определение:** Используются в компонентах, хуках, утилитах во время работы приложения

**Примеры:**
- Компоненты импортируют `lucide-react`
- Формы используют `react-hook-form`
- API клиенты используют `@supabase/supabase-js`

**Где добавлять:**
- ✅ ТОЛЬКО в `shared/package.json`
- ❌ НЕ дублировать в приложениях

**Почему не дублировать:**
- Загружаются через `workspace:*` в runtime
- Единая версия для всех приложений
- Меньше размер bundle
- Проще управление версиями

**Текущий список:**
```json
// shared/package.json
{
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1",
    "@supabase/ssr": "^0.8.0",
    "react-hook-form": "^7.71.1",
    "zod": "^4.3.5",
    "lucide-react": "^0.562.0",
    "@radix-ui/react-*": "^*",
    "tailwind-merge": "^3.4.0",
    "clsx": "^2.1.1"
  }
}
```

---

#### ⚙️ Framework зависимости

**Определение:** Основные фреймворки, требуемые для работы Next.js приложения

**Где добавлять:**
- ✅ В `package.json` КАЖДОГО приложения
- ❌ НЕ добавлять в shared (это не библиотека)

**Список:**
```json
{
  "dependencies": {
    "next": "16.1.1",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  }
}
```

---

## 🎯 Практическое руководство

### Добавление новой зависимости

#### Шаг 1: Определите тип

**Вопрос 1:** Где используется зависимость?
- В `next.config.ts` или `middleware.ts` → **Build-time**
- В компонентах, хуках, утилитах → **Runtime**
- `next`, `react`, `react-dom` → **Framework**

**Вопрос 2:** Кто использует?
- Только одно приложение → Добавить в это приложение
- Несколько приложений → Добавить в shared
- Конфигурационные файлы → Добавить везде

#### Шаг 2: Добавьте зависимость

**Build-time зависимость:**
```bash
# Добавить во все приложения
pnpm --filter site add package-name
pnpm --filter admin add package-name
pnpm --filter workspace add package-name
pnpm --filter platform add package-name

# И в shared (если используется в shared коде)
pnpm --filter shared add package-name
```

**Runtime зависимость:**
```bash
# Добавить только в shared
pnpm --filter shared add package-name
```

**Framework зависимость:**
```bash
# Добавить во все приложения
pnpm --filter site add package-name
pnpm --filter admin add package-name
pnpm --filter workspace add package-name
pnpm --filter platform add package-name
```

#### Шаг 3: Проверьте

```bash
# Полная переустановка и проверка
./reset-deps.sh

# Или вручную
pnpm install
pnpm build:all
```

---

## ❌ Типичные ошибки и решения

### Ошибка 1: "Cannot find module 'X' in next.config.ts"

**Симптомы:**
```
⨯ Failed to load next.config.ts
Error: Cannot find module 'next-intl/plugin'
```

**Причина:** Build-time зависимость добавлена только в `shared/package.json`

**Решение:**
```bash
# Добавить во все приложения
pnpm --filter site add next-intl
pnpm --filter admin add next-intl
pnpm --filter workspace add next-intl
pnpm --filter platform add next-intl

./reset-deps.sh
```

---

### Ошибка 2: "Cannot find name 'process'"

**Симптомы:**
```typescript
// В любом файле
process.env.NEXT_PUBLIC_URL
// ^^^^^^^ Cannot find name 'process'
```

**Причина:** Отсутствует `"types": ["node"]` в `tsconfig.json`

**Решение:**
```json
// app/tsconfig.json
{
  "compilerOptions": {
    "types": ["node"]  // ← Добавить
  }
}
```

Или наследовать от базового:
```json
{
  "extends": "../tsconfig.base.json"  // Уже содержит types: ["node"]
}
```

---

### Ошибка 3: Дублирование зависимостей в bundle

**Симптомы:**
- Большой размер bundle
- `pnpm why package-name` показывает множество версий
- Предупреждения о дубликатах при сборке

**Причина:** Runtime зависимость добавлена и в приложения, и в shared

**Решение:**
```bash
# Удалить из приложений
pnpm --filter site remove package-name
pnpm --filter admin remove package-name
pnpm --filter workspace remove package-name
pnpm --filter platform remove package-name

# Оставить только в shared (если это runtime зависимость)
# Или добавить во все (если это build-time зависимость)

./reset-deps.sh
```

---

### Ошибка 4: "shared" module not found

**Симптомы:**
```typescript
import { Button } from "shared/components/ui/button";
// ^^^^^^ Cannot find module 'shared'
```

**Причина:** 
- Не выполнен `pnpm install` после изменений
- Сломанный symlink
- Проблема с pnpm workspace

**Решение:**
```bash
./reset-deps.sh
# Перезапустить TypeScript Server в IDE
```

---

## 🔍 Проверка здоровья монорепозитория

### Чек-лист перед коммитом

```bash
# 1. Проверить, что build-time зависимости во всех приложениях
grep -r "next-intl" site/package.json admin/package.json workspace/package.json platform/package.json

# 2. Проверить сборку всех приложений
pnpm build:all

# 3. Проверить TypeScript
pnpm --filter site run type-check
pnpm --filter admin run type-check
pnpm --filter workspace run type-check
pnpm --filter platform run type-check

# 4. Проверить линтер
pnpm lint
```

### Автоматическая проверка

**GitHub Actions CI** автоматически проверит:
- ✅ Установку зависимостей
- ✅ Сборку всех приложений
- ✅ Наличие критичных зависимостей
- ✅ TypeScript ошибки

---

## 🛠️ Утилиты и команды

### Полезные команды

```bash
# Переустановка зависимостей
./reset-deps.sh

# Сборка всех приложений
pnpm build:all

# Сборка одного приложения
pnpm --filter site build

# Запуск dev сервера
pnpm dev:site       # site (port 3000)
pnpm dev:admin      # admin (port 3001)
pnpm dev:workspace  # workspace (port 3002)
pnpm dev:platform   # platform (port 3003)
pnpm dev:all        # все одновременно

# Проверка зависимостей
pnpm list next-intl --depth=0                  # во всех пакетах
pnpm --filter site list next-intl --depth=0   # в конкретном пакете
pnpm why next-intl                             # кто использует и почему

# Добавление зависимости
pnpm --filter site add package-name            # в site
pnpm --filter shared add package-name          # в shared
pnpm add -D package-name                       # в корень (devDependencies)

# Удаление зависимости
pnpm --filter site remove package-name
```

---

## 📚 Дополнительные ресурсы

### Документация

- [VERCEL_SETUP.md](../VERCEL_SETUP.md) - Настройка деплоя на Vercel
- [VERCEL_DEPLOYMENT.md](./VERCEL_DEPLOYMENT.md) - Подробное руководство по деплою
- [pnpm workspace](https://pnpm.io/workspaces) - Официальная документация

### Структура shared пакета

```
shared/
├── auth-sdk/           → Утилиты авторизации (Supabase)
├── components/         → UI компоненты (Radix UI + shadcn)
│   └── ui/            → Базовые UI компоненты
├── lib/               → Утилиты, хелперы
│   ├── i18n/         → Интернационализация
│   └── api/          → API клиенты
├── hooks/            → React хуки
├── providers/        → React провайдеры
├── types/            → TypeScript типы
└── index.ts          → Публичный API
```

---

## 🎓 Лучшие практики

### DO (Делайте так)

✅ Используйте базовый `tsconfig.base.json` для общих настроек  
✅ Дублируйте build-time зависимости во всех приложениях  
✅ Держите runtime зависимости только в shared  
✅ Запускайте `./reset-deps.sh` после изменения зависимостей  
✅ Проверяйте сборку всех приложений перед коммитом  
✅ Используйте `workspace:*` для ссылки на shared  
✅ Обновляйте зависимости синхронно во всех пакетах

### DON'T (Не делайте так)

❌ Не добавляйте runtime зависимости в приложения  
❌ Не забывайте добавлять build-time зависимости во все приложения  
❌ Не меняйте `.npmrc` без понимания последствий  
❌ Не коммитьте без проверки `pnpm build:all`  
❌ Не изменяйте версии зависимостей выборочно  
❌ Не импортируйте напрямую из `../shared/`, используйте `shared/`

---

## 🚨 Экстренная помощь

### Всё сломалось, что делать?

```bash
# 1. Полная очистка
rm -rf node_modules */node_modules pnpm-lock.yaml
rm -rf .next */.next

# 2. Переустановка
pnpm install

# 3. Проверка
pnpm build:site

# 4. Если не помогло - проверьте package.json
# Убедитесь что:
# - next-intl есть во всех приложениях
# - shared: "workspace:*" есть во всех приложениях
# - Нет конфликтующих версий

# 5. Перезапустите TypeScript Server в IDE
```

### Получить помощь

1. Проверьте [VERCEL_DEPLOYMENT.md](./VERCEL_DEPLOYMENT.md)
2. Запустите `./reset-deps.sh` и проверьте вывод
3. Проверьте GitHub Actions CI на наличие ошибок
4. Обратитесь к команде

---

**Последнее обновление:** 2026-01-16  
**Авторы:** Oblikflow Development Team
