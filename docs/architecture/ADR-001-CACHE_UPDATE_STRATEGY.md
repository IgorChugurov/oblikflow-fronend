# ADR-001: Стратегия обновления кэша React Query

**Статус:** ✅ Принято  
**Дата:** 2026-01-19  
**Авторы:** Igor Chugurov  

---

## Контекст и проблема

В приложении используется React Query для кэширования данных списков и детальных представлений сущностей. После операций мутации (CREATE/UPDATE/DELETE) необходимо обновить кэш, чтобы пользователь видел актуальные данные.

### Проблемы с предыдущим подходом

**Проблема 1: Несоответствие ключей кэша**
```typescript
// В мутации инвалидировали:
queryClient.invalidateQueries({ queryKey: ['enterprises'] });

// Но в списке использовался ключ:
queryKey: ['list', 'admin', 'enterprises', params]

// Результат: кэш не обновлялся ❌
```

**Проблема 2: Потеря позиции пользователя**
При инвалидации кэша пользователь терял:
- Текущую страницу (возврат на страницу 1)
- Примененные фильтры
- Поисковый запрос
- Настройки сортировки

**Проблема 3: Дополнительные запросы к API**
После каждой мутации происходила полная перезагрузка списка, даже если изменений было мало.

---

## Рассмотренные варианты

### Вариант 1: Оптимистичные обновления (Optimistic Updates)

**Принцип:** Мгновенно обновлять кэш на клиенте, не дожидаясь ответа сервера.

```typescript
onSuccess: (updatedItem) => {
  // Находим все страницы списка
  const queries = queryClient.getQueriesData({ queryKey: ['list', ...] });
  
  // Обновляем элемент во всех страницах
  queries.forEach(([key, data]) => {
    const updated = data.data.map(item => 
      item.id === updatedItem.id ? updatedItem : item
    );
    queryClient.setQueryData(key, { ...data, data: updated });
  });
}
```

**Плюсы:**
- ⚡ Мгновенная реакция UI
- ✅ Сохранение позиции пользователя
- 🚀 Нет дополнительных запросов

**Минусы:**
- ⚠️ Риск рассинхронизации с сервером
- ⚠️ Клиент не видит server-computed поля (timestamps, автонумерация, вычисляемые суммы)
- ⚠️ Сложность реализации для вложенных структур
- ⚠️ Проблемы при серверной валидации

### Вариант 2: Инвалидация с потерей позиции (текущий)

**Принцип:** Помечать кэш как устаревший и перезагружать с первой страницы.

```typescript
onSuccess: () => {
  queryClient.invalidateQueries({ queryKey: ['enterprises'] });
}
```

**Плюсы:**
- ✅ Простота реализации
- ✅ Гарантия актуальности

**Минусы:**
- ❌ Потеря позиции пользователя
- ❌ Плохой UX
- ❌ Не решает проблему несоответствия ключей

### Вариант 3: Safe Refetch (выбранное решение)

**Принцип:** Перезагружать данные с сервера, сохраняя параметры запроса.

```typescript
onSuccess: async () => {
  await queryClient.refetchQueries({ 
    queryKey: ['list', projectId, serviceType],
    exact: false,      // все вариации (разные params)
    type: 'active'     // только активные запросы
  });
}
```

**Плюсы:**
- ✅ Сохранение позиции пользователя (page, search, filters)
- ✅ Гарантия актуальности данных (прямо с сервера)
- ✅ Простота реализации
- ✅ Подходит для всех типов данных (справочники и финансы)
- ✅ Нет риска рассинхронизации
- ✅ Видны все server-computed поля

**Минусы:**
- ⏱️ Небольшая задержка (0.5-1 сек) - приемлемо
- 📡 Дополнительный запрос к API (но целенаправленный)

---

## Решение

**Принята стратегия: Safe Refetch**

Используем перезагрузку данных с сервера с сохранением позиции пользователя для всех операций (CREATE/UPDATE/DELETE) на всех типах сущностей (справочники и финансовые документы).

### Архитектура решения

#### 1. Централизованные ключи кэша

```typescript
// shared/lib/api/core/query-keys.ts

export const listKeys = {
  all: (projectId: string, serviceType: string) => 
    ['list', projectId, serviceType] as const,
  
  page: (projectId: string, serviceType: string, params: any) => 
    ['list', projectId, serviceType, params] as const,
};

export const detailKeys = {
  enterprise: (id: string) => ['enterprise', id] as const,
  // ... другие типы
};
```

#### 2. Универсальная функция обновления

```typescript
// shared/lib/api/core/cache-manager.ts

export async function updateListCache(options: {
  queryClient: QueryClient;
  projectId: string;
  serviceType: string;
}): Promise<void> {
  const { queryClient, projectId, serviceType } = options;
  
  const baseQueryKey = listKeys.all(projectId, serviceType);
  
  await queryClient.refetchQueries({ 
    queryKey: baseQueryKey,
    exact: false,   // все страницы
    type: 'active'  // только видимые
  });
}
```

#### 3. Использование в хуках

```typescript
// shared/api/hooks/enterprises/useUpdateEnterprise.ts

export function useUpdateEnterprise(enterpriseId: string, projectId = 'admin') {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async (data) => {
      const result = await enterprisesSDK.update(enterpriseId, data);
      if (result.error) throw new Error(result.error.message);
      return result.data!;
    },
    
    onSuccess: async (response) => {
      // Обновляем детали
      updateDetailCache({
        queryClient,
        detailKey: detailKeys.enterprise(enterpriseId),
        data: response,
      });
      
      // Обновляем списки (safe-refetch)
      await updateListCache({
        queryClient,
        projectId,
        serviceType: 'enterprises',
      });
    },
  });
}
```

---

## Последствия

### Положительные

1. **Консистентность данных**
   - Клиент всегда показывает данные, идентичные серверным
   - Нет риска показать устаревшую или неполную информацию

2. **Лучший UX**
   - Пользователь остается на той же странице
   - Фильтры и поиск сохраняются
   - Позиция прокрутки не теряется

3. **Универсальность**
   - Один подход для всех типов данных
   - Простота в понимании и поддержке
   - Легко масштабировать на новые сущности

4. **Надежность**
   - Подходит для критичных данных (финансовые документы)
   - Показывает точные вычисленные поля (суммы, НДС, номера)
   - Нет проблем с серверной валидацией

### Негативные

1. **Небольшая задержка**
   - Обновление занимает 0.5-1 секунду вместо мгновенного
   - Решение: можно добавить loading индикатор при необходимости

2. **Дополнительный запрос к API**
   - После каждой мутации запрос к серверу
   - Решение: запрос целенаправленный (только активная страница)

### Альтернативы для будущего

В будущем можно рассмотреть гибридный подход для некритичных справочников:
- Оптимистичное обновление для мгновенного отклика
- Фоновая синхронизация через 2-3 секунды
- Но это добавит сложности, поэтому пока не требуется

---

## Примеры использования

### Для справочников (Enterprises, Counterparties, Products)

```typescript
export function useUpdateCounterparty(id: string, projectId: string) {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async (data) => {
      const result = await counterpartiesSDK.update(id, data);
      if (result.error) throw new Error(result.error.message);
      return result.data!;
    },
    
    onSuccess: async (response) => {
      updateDetailCache({
        queryClient,
        detailKey: ['counterparty', id],
        data: response,
      });
      
      await updateListCache({
        queryClient,
        projectId,
        serviceType: 'counterparties',
      });
    },
  });
}
```

### Для финансовых документов (Invoices, Payments)

```typescript
export function useUpdateInvoice(id: string, projectId: string) {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async (data) => {
      const result = await invoicesSDK.update(id, data);
      if (result.error) throw new Error(result.error.message);
      return result.data!;
    },
    
    onSuccess: async (response) => {
      updateDetailCache({
        queryClient,
        detailKey: ['invoice', id],
        data: response,
      });
      
      // Та же стратегия - safe-refetch
      await updateListCache({
        queryClient,
        projectId,
        serviceType: 'invoices',
      });
    },
  });
}
```

---

## Связанные решения

- [Документация React Query](https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates)
- `docs/architecture/CACHE_UPDATE_STRATEGY.md` - детальная документация
- `shared/lib/api/core/cache-manager.ts` - реализация
- `shared/lib/api/core/query-keys.ts` - определения ключей

---

## История изменений

- **2026-01-19**: Первая версия - выбрана стратегия Safe Refetch
