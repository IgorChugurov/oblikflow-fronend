# 🔧 Быстрое исправление TypeScript ошибок

## 🚨 Проблема
Видите ошибку: `Property 'children' does not exist on type ThemeProviderProps`?

## ✅ Решение

Выполните эти команды **в корне проекта**:

```bash
# Остановите dev сервер если запущен (Ctrl+C)

# Запустите скрипт очистки
./reset-deps.sh

# Или вручную:
rm -rf node_modules */node_modules pnpm-lock.yaml .next */.next
pnpm install
```

## 🔄 Перезапустите TypeScript Server

### VS Code
1. Нажмите `Cmd+Shift+P` (Mac) или `Ctrl+Shift+P` (Windows/Linux)
2. Введите: `TypeScript: Restart TS Server`
3. Нажмите Enter

### Или просто перезапустите VS Code

## 📝 Почему это происходит?

После очистки дублирующихся зависимостей из `package.json` файлов:
- TypeScript использует устаревшие типы из старого кеша
- `next-themes` теперь в `shared/node_modules`, но симлинки не обновлены
- После `pnpm install` все работает правильно

## ✅ Проверка

После выполнения команд:
```bash
# Запустите dev сервер
pnpm dev:site

# Не должно быть TypeScript ошибок
```

---

**Это одноразовая процедура!** После переустановки всё будет работать нормально.
